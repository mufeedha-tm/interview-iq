# Frontend

This is the React frontend for InterviewIQ.

## Stack

- React
- Vite
- Tailwind CSS
- React Router
- Axios

## Run

Create `frontend/.env` and add:

- `VITE_API_BASE_URL`
- `VITE_STRIPE_PUBLISHABLE_KEY`

Then run:

```bash
npm install
npm run dev
```
